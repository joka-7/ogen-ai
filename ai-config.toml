[project]
name = "ogen-ai"

[stack]
# No language/framework fragments selected yet — this repo has no code committed.
# Add entries here (matching .ai/rules/{languages,frameworks}/<name>.md) once the
# stack is decided, then re-run ai-sync.
languages  = []
frameworks = []
practices  = ["testing", "git-commits", "security"]

[tools]
targets = ["claude", "cursor", "gemini", "copilot"]

[options]
link_mode = "symlink"
local_tail = "ai-config.local.md"
cursor_mdc = false
